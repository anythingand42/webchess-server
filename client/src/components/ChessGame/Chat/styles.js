import styled from "styled-components";

const Styles = {
    ChatDiv: styled.div`
        display: grid;
        grid-template-rows: 1fr auto;
        height: 100%;
        width: 100%;
    `
};

export default Styles;